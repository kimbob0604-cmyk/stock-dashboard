# 📋 주식 대시보드 — 현황 정리 & 재설계 브리프

> 새 Claude 대화에 이 문서를 통째로 붙여넣고 "이 시스템을 아래 새 방향으로 재설계해줘"라고 요청하면 됩니다.

---

## 1. 한 줄 요약

한국(+미국) 주식 대시보드. **Flask(Python) 단일 서버 + SQLite + 바닐라 JS 프론트**,
Render 무료플랜 배포, 텔레그램으로 시황/알림 push. 첫 자작 프로젝트라 기능은 많지만
구조가 산만하고(server.py 단일 파일 ~1.6만 줄, pages.js ~9천 줄) 알림이 **가격 기준에만**
머물러 있음.

## 2. 기술 스택 / 인프라

| 영역 | 내용 |
|---|---|
| 백엔드 | Python / Flask, `server.py` 단일 파일(약 16,000줄), API 약 145개 |
| DB | SQLite (`db/dashboard.db`) — stocks, flow_cache, chart_cache, financial, disclosure 등 |
| 프론트 | 바닐라 JS (`static/js/*.js`, pages.js ~9천 줄), D3, socket.io. SPA 라우팅 |
| 스케줄러 | APScheduler, cron 약 39개 (TZ=Asia/Seoul) |
| 배포 | Render 무료플랜 (gunicorn). **영속 디스크 없음** → 재시작마다 cache/·DB 휘발 |
| 데이터 영속 | 핵심 테이블만 GitHub Gist 백업/복원, 종목 유니버스는 git 커밋 시드 |
| 알림 | 텔레그램 (push 11종 + 양방향 봇 명령) |
| 데이터 소스 | Naver금융(스크랩+polling API), KRX OpenAPI, pykrx, DART, yfinance, Finnhub, KIS(한국투자증권) |

## 3. 현재 기능 (카테고리별)

**프론트 페이지(사이드바)**
- 시장: 대시보드, 테마맵(트리맵), 밸류체인, 시황분석, 글로벌매크로, 공시
- 분석: 종목 통합 상세, 검증 시트, 분석 일지, 종목발굴, AI추천, 관심종목, 백테스트, 추천성과, 상관관계
- 운영: 데이터 신선도, Cron 모니터, 헬스 대시보드
- 매매: 포트폴리오, 수익률 저널 / 기타: 리서치, ETF맵, 배당

**자동화(주요 cron)**
- 가격 동기화(장중 30분/마감/시간외), 수급 배치, 유니버스 빌드
- AI 에이전트 파이프라인, 어닝 파이프라인, 컨센서스 스냅샷·리비전
- DART 공시 폴링, DB Gist 백업, **데이터 워치독(자가복구)**

**텔레그램**
- push: 아침 브리핑, 저녁 확정 시황(19:00), 수급 시그널(19:30), 미국 장마감, 관심종목 가격, 신규 리포트, 리비전, 익일 예측, 트레일링 스톱
- 양방향 봇: `/시황` `/시그널` `/수급 <종목>` `/가격 <종목>` `/도움`

## 4. ⭐ 재설계 시 재활용할 핵심 자산 (이미 만들어져 있음)

> 새 시스템은 이걸 **버리지 말고 엔진의 입력으로 재사용**해야 함.

**(A) 기술적 지표 계산 — 이미 구현되어 있으나 "차트 표시용"으로만 쓰임**
- `_calc_rsi_macd()` : RSI(14) + MACD(12,26,9) — 순수 Python
- `_calc_bollinger()` : 볼린저 밴드
- `_calc_adx()` : ADX(추세 강도)
- `_calc_fibonacci()`, `_calc_trendlines()` : 피보나치·추세선
- `/api/volume_profile/<code>` : POC / Value Area / VWAP / 지지·저항
- 52주 신고가 근접 스캐너
- → **이 지표들이 알림 조건엔 아직 연결돼 있지 않음** (현재 알림은 가격/등락률만)

**(B) 데이터 캐시 (조건 평가의 원천)**
- `chart_cache` (OHLCV 일봉, 종목별) / `/api/chart/<code>`
- `flow_cache` (외국인·기관 20일 수급 시계열)
- `financial` (PER/PBR/EPS 등) / `stocks` (실시간 가격·등락·거래대금)

**(C) 알림·관심종목 인프라 (확장 대상)**
- `alert_rules` (cache/alert_rules.json + DB) — 규칙 저장
- `check_alert_rules()` cron(장중 10분) — **현재 지원 조건: price_above/below, change_up/down 만**
- 관심종목(watchlist) localStorage + 서버 동기화
- 트레일링 스톱, 쿨다운(중복 알림 방지) 로직
- `send_telegram()` + 양방향 봇

## 5. 현재의 한계 (솔직한 평가)

- **알림이 가격 기준뿐** — 기술적 분석(RSI 과매도, MACD 골든크로스, 볼린저 돌파 등) 조건 알림이 없음
- server.py 단일 파일 ~1.6만 줄 → 유지보수 어려움 (모듈 분리 필요)
- 조건/규칙이 코드에 하드코딩, 사용자가 UI로 자유 조합 불가
- 알림 발생 후 **사후 검증/성과 추적**이 약함

---

## 6. 🎯 새로 만들고 싶은 것 (핵심 목표)

> **"기술적 분석 근거를 조건으로 정의 → 관심종목 등록 → 조건 충족 시 알림"**
> 인 **규칙 기반 기술적 알림 엔진**.

### 6.1 사용자 시나리오
1. 관심종목을 등록한다 (예: 삼성전자, SK하이닉스, 한미반도체…)
2. 각 종목 또는 전체에 **기술적 조건**을 건다.
   - 예: "RSI(14) < 30 (과매도)" / "MACD 골든크로스" / "종가가 볼린저 하단 터치"
   - 예: "20일선 상향 돌파" / "거래량 20일 평균 대비 2배" / "외국인 3일 연속 순매수"
   - 여러 조건 AND/OR 조합
3. 장중/장마감 주기적으로 조건을 평가한다.
4. **충족되면 텔레그램 알림** (종목명 + 어떤 조건이 충족됐는지 + 현재 지표값 + 차트 링크).
5. 알림 이력과 그 이후 주가 성과를 추적한다(선택).

### 6.2 원하는 조건 종류 (예시 — 자유 조합)
- **가격/이평선**: 골든/데드크로스(5·20·60일), N일선 돌파, 신고가/신저가
- **모멘텀**: RSI 과매수/과매도, RSI 다이버전스, MACD 크로스/히스토그램 전환
- **변동성**: 볼린저 밴드 상·하단 터치/돌파, 밴드 스퀴즈
- **추세**: ADX > 25(추세 강함), 추세선 돌파
- **거래량/수급**: 거래량 급증, 외국인·기관 연속/쌍끌이 순매수, 수급 반전
- **펀더멘털 필터(선택)**: PER/PBR 밴드, 시총 하한

### 6.3 설계 시 결정해야 할 것 (새 Claude에게 물어볼 포인트)
1. **아키텍처**: 기존 Flask를 모듈화해 확장? 아니면 새 구조(예: FastAPI + 규칙엔진 분리)?
2. **규칙 스키마**: 조건을 어떻게 데이터로 표현할지 (JSON DSL 예시 ↓), UI에서 어떻게 조합시킬지
3. **평가 엔진**: 장중 폴링 주기 / 지표 계산 캐싱 / 백테스트로 조건 검증
4. **알림 정책**: 쿨다운, 조건별 우선순위, 충족 근거 메시지 포맷
5. **성과 추적**: 알림 후 N일 수익률 기록 → 어떤 조건이 잘 맞는지 통계
6. **Render 무료플랜 제약**(비영속 디스크) 위에서 규칙·이력을 어떻게 영속화할지

### 6.4 규칙 스키마 초안 (논의용)
```json
{
  "name": "반도체 과매도 반등",
  "universe": ["005930", "000660", "042700"],
  "logic": "AND",
  "conditions": [
    { "indicator": "rsi", "params": {"period": 14}, "op": "<", "value": 30 },
    { "indicator": "ma_cross", "params": {"fast": 5, "slow": 20}, "op": "==", "value": "golden" },
    { "indicator": "foreign_streak", "params": {"side": "buy"}, "op": ">=", "value": 3 }
  ],
  "alert": { "channel": "telegram", "cooldown_hours": 24 },
  "track_performance": true
}
```

---

## 7. 새 Claude에게 줄 요청문 (복붙용)

> 위 문서는 내가 처음 만든 주식 대시보드의 현황이야. 핵심은 **6번 목표** —
> "기술적 분석 조건을 정의하고, 관심종목에 걸어서, 조건이 충족되면 텔레그램 알림을 주는
> 규칙 기반 알림 시스템"을 제대로 만들고 싶어.
>
> 4번의 기존 자산(이미 구현된 RSI/MACD/볼린저/ADX 계산, OHLCV·수급 캐시, 알림/관심종목/
> 텔레그램 인프라)은 최대한 재사용하고 싶어. 6.3의 결정 포인트들을 같이 정해주고,
> 규칙 스키마(6.4)와 평가 엔진, UI, 성과 추적까지 포함한 **단계별 구현 계획**을 제안해줘.
> Render 무료플랜(비영속 디스크) 제약도 고려해줘.
